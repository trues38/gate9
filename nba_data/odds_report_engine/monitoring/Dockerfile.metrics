FROM python:3.10-slim

WORKDIR /app

# 필요한 패키지 설치
RUN pip install --no-cache-dir \
    flask \
    prometheus-client \
    neo4j \
    python-dotenv \
    requests

# 메트릭 API 복사
COPY metrics_api.py /app/

# Flask 실행
CMD ["python", "metrics_api.py"]
